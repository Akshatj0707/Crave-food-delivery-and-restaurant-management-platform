# On Cyclic — frontend and backend are on same domain
# So no need to set this — it uses /api automatically
# Only set this if deploying frontend separately
# REACT_APP_API_URL=https://your-app.cyclic.app/api

# Stripe publishable key
REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_test_placeholder
