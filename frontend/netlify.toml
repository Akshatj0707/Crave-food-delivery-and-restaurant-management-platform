[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"
  CI = "false"

# SPA routing — all routes go to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
